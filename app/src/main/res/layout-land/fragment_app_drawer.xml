<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Optional opacity scrim between the wallpaper and the drawer UI. -->
    <View
        android:id="@+id/drawerOpacityScrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="true"
        android:background="?attr/primaryShadeDarkColor"
        android:orientation="vertical">

    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="24dp"
        android:layout_weight="0.1" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="56dp"
        android:animateLayoutChanges="true"
        android:orientation="horizontal">

        <androidx.appcompat.widget.SearchView
            android:id="@+id/search"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="end"
            android:imeOptions="actionSearch"
            android:paddingHorizontal="8dp"
            android:paddingVertical="0dp"
            android:textCursorDrawable="@null"
            app:closeIcon="@null"
            app:iconifiedByDefault="false"
            app:queryBackground="@null"
            app:queryHint="___"
            app:searchIcon="@null"
            app:theme="@style/AppSearchText" />

        <!-- Drawer-local search options (Android settings / contacts). Shown only in the
             main launch drawer; replaces the toggles that used to live in Settings. -->
        <ImageView
            android:id="@+id/searchOptions"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="12dp"
            android:background="@drawable/circle_shade_color_glass"
            android:foreground="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/search_drawer_title"
            android:padding="8dp"
            android:src="@drawable/ic_tune"
            android:visibility="gone"
            app:tint="?attr/primaryColor" />

        <TextView
            android:id="@+id/appRename"
            style="@style/TextSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="24dp"
            android:paddingVertical="@dimen/app_padding_vertical"
            android:text="@string/rename"
            android:textAllCaps="true"
            android:visibility="gone" />

    </LinearLayout>

    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.06" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="56dp"
        android:layout_weight="1"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:fadingEdgeLength="24dp"
        android:requiresFadingEdge="vertical"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

    </LinearLayout>

</FrameLayout>